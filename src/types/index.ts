export type Html5Topic = {
    title: string;
    description: string;
    examples?: string[];
};

export type Css3Topic = {
    title: string;
    description: string;
    examples?: string[];
};

export type ScssTopic = {
    title: string;
    description: string;
    examples?: string[];
};